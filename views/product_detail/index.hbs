<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Termékek | Protex</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .product-detail-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 0;
      box-sizing: border-box;
      margin-bottom: -10%;
    }

    .product {
      display: flex;
      border-radius: 5px;
      width: 100%;
      height: 100%;
    }

    .product-image {
      flex: 1;
      padding-right: 20px;
    }

    .product-image img {
      max-width: 100%;
      max-height: auto;
      border-radius: 5px;
      object-fit: cover;
    }

    .product-details {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .product-title {
      font-size: 28px;
      font-weight: bold;
      text-align: center;
      margin-bottom: 10px;
      margin-top: -15%;
    }

    .product-description {
      font-size: 20px;
      text-align: center;
      margin-bottom: 10px;
    }

    .product-price {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .white-button {
      background-color: #ffffff;
      border: 2px solid #cccccc;
      border-radius: 4px;
      color: #333333;
      cursor: pointer;
      font-size: 16px;
      font-weight: bold;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      transition: all 0.3s ease;
      text-decoration: none;
      text-underline-offset: none;
    }

    .white-button:hover {
      background-color: #f5f5f5;
      border-color: #999999;
      color: #111111;
    }

    a {
      text-decoration: none;
    }

    /* Media queries */
    @media screen and (max-width: 767px) {
      /* For phones */
      .product {
        flex-direction: column;
      }

      .product-image {
        padding-right: 0;
        padding-bottom: 20px;
      }

      .product-details {
        margin-top: 20px;
      }

      .product-title {
        font-size: 24px;
      }

      .product-description {
        font-size: 18px;
      }

      .product-price {
        font-size: 18px;
      }

      .white-button {
        font-size: 14px;
      }
    }

    @media screen and (min-width: 768px) and (max-width: 1023px) {
      /* For tablets */
      .product {
        flex-direction: column;
      }

      .product-image {
        padding-right: 0;
        padding-bottom: 20px;
      }

      .product-details {
      margin-top: 20px;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .product-title {
      font-size: 28px;
      font-weight: bold;
      text-align: center;
      margin-bottom: 10px;
      margin-top: -15%;
    }

    .product-description {
      font-size: 20px;
      text-align: center;
      margin-bottom: 10px;
    }

    .product-price {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .white-button {
      background-color: #ffffff;
      border: 2px solid #cccccc;
      border-radius: 4px;
      color: #333333;
      cursor: pointer;
      font-size: 16px;
      font-weight: bold;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      transition: all 0.3s ease;
      text-decoration: none;
      text-underline-offset: none;
    }

    .white-button:hover {
      background-color: #f5f5f5;
      border-color: #999999;
      color: #111111;
    }

    a {
      text-decoration: none;
    }
  </style>
</head>

<body>
  <div class="product-detail-container">
    <div class="product">
      <div class="product-image">
        <img src="/img/products/{{product.img_url}}" alt="Product Image">
      </div>
      <div class="product-details">
        <div class="product-title">{{product.name}}</div><br>
        <div class="stock-status">
              {{#if in_stock}}
              <div class="in-stock">Készleten</div>
              {{else}}
              <div class="out-of-stock">*Nincs implementálva a raktár</div>
              {{/if}}
            </div><br><br>
        <div class="product-description">{{product.description}}</div><br><br>
        <div class="product-price">{{product.price}}Ft</div><br><br>
        <a href="/add-to-cart/{{product.id}}">
          <div class="white-button">Vásárlás</div>
        </a>
      </div>
    </div>
  </div>
  <div class="suggested-products">
    <div class="container">
      <div class="suggested-products-title">Ajánlott termékek</div>
      <div class="suggested-products-container">
        {{#each suggestedProducts}}
        <div class="product">
          <div class="product-image">
            <img src="/img/products/{{img_url}}" alt="Product Image">
          </div>
          <div class="product-details">
            <div class="product-title">{{name}}</div><br><br>
            <div class="product-description">{{description}}</div><br><br>
            <div class="product-price">{{price}}Ft</div><br><br>
            <a href="/add-to-cart/{{id}}">
              <div class="white-button">Vásárlás</div>
            </a>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
</body>

</html>